<resources>

    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/app_theme_primary</item>
        <item name="colorPrimaryDark">@color/app_theme_primary_dark</item>
        <item name="colorAccent">@color/app_theme_accent</item>
    </style>

    <!-- Toolbar -->

    <style name="AppTheme.Toolbar">
        <item name="popupTheme">@style/Theme.AppCompat.NoActionBar</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="android:background">@color/app_toolbar_background</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Loading -->

    <style name="AppTheme.Loading">
        <item name="android:background">@color/app_loading_background</item>
    </style>

    <style name="AppTheme.Loading.Circular" parent="@style/Widget.AppCompat.ProgressBar">
        <item name="android:layout_width">@dimen/app_loading_circular_width</item>
        <item name="android:layout_height">@dimen/app_loading_circular_height</item>
    </style>

</resources>
